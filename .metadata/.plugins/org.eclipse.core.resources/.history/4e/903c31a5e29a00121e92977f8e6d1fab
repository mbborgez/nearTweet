package main.java.com.cm.server;


public class Server 
{	
	public static void main(String args[])
	{
		if(args.length<2){
			System.err.println("Usage: Server <server name>" );
			return;
		}
		ServerProcess server = new ServerProcess(args[0], 8000);
		server.run();
		server.close();
	}
}
